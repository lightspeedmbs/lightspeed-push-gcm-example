<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="25dp"
    android:paddingRight="25dp"
    android:background="@drawable/bg_720x1280" >
    <ImageButton
        android:id="@+id/buttonLogin"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/selector_login_button"
        android:background="@null"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_marginBottom="80dp"
        />
    <ImageView
        android:id="@+id/imgPass"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_above="@id/buttonLogin"
        android:layout_marginBottom="7dp"
        android:background="@null"
        android:src="@drawable/bu_password"
        />
    <ImageView
        android:id="@+id/imgName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@id/imgPass"
        android:layout_marginBottom="7dp"
        android:layout_alignParentLeft="true"
        android:src="@drawable/bu_email"
        android:background="@null"/>

    <EditText
        android:id="@+id/editName"
        android:layout_width="270dp"
        android:layout_height="50dp"
        android:layout_toRightOf="@id/imgName"
        android:layout_alignBottom="@id/imgName"
        android:ems="10"
        android:hint="mymail@mail.com"
        android:paddingLeft="10dp"
        android:inputType="textEmailAddress"
        android:textColor="#939393"
        android:textSize="15sp"
        android:background="#323232" >
        <requestFocus />
    </EditText>

    <EditText
        android:id="@+id/editPass"
        android:layout_width="270dp"
        android:layout_height="50dp"
        android:layout_toRightOf="@id/imgPass"
        android:layout_alignBottom="@id/imgPass"
        android:ems="10"
        android:hint="password"
        android:paddingLeft="10dp"
        android:inputType="textPassword" 
        android:background="#323232"
		android:textColor="#939393"
        android:textSize="15sp"/>
        

    <TextView
        android:id="@+id/showResult"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="#FFFFFF"
        android:textSize="15sp"
        android:layout_alignParentBottom="true"
        android:layout_centerInParent="true"
        android:layout_marginBottom="15dp"
        
        ></TextView>
 
</RelativeLayout>